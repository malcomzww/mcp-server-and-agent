"""A hand-written MCP server at the protocol level, four agent topologies over
it, and the failure rate of each measured under a controlled fault model.

The agent brain is a deterministic simulation, not a live model. See
``faults.py`` for why that is the instrument rather than a shortcut.
"""

__version__ = "0.1.0"
